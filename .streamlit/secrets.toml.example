# Copy to .streamlit/secrets.toml for local live enrichment (git-ignored).
# On Render, set these as environment variables instead.
OPENAI_API_KEY = "sk-..."

# Optional: persist enrichment tags in Supabase (else local disk JSON).
# SUPABASE_URL = "https://<project>.supabase.co"
# SUPABASE_KEY = "<service-role-key>"
