# Copy to .env (local) or set in Render / GitHub secrets.
# The matcher runs fully offline from the committed cache without these.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Optional: persist enrichment tags in Supabase (else local disk JSON).
SUPABASE_URL=
SUPABASE_KEY=
